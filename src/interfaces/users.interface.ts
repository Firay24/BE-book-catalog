export interface UserResponse {
  id: string;
  email: string;
  password: string;
  name: string;
  roleId: string;
}

export interface UserAuth {
  id: string;
  email: string;
  name: string;
  roleId: string;
}
